'use strict';
/* Animated dot-grid background */
(function(){const c=document.getElementById('gridCanvas'),ctx=c.getContext('2d');let w,h;const sp=40;function resize(){w=c.width=window.innerWidth;h=c.height=window.innerHeight;}resize();window.addEventListener('resize',resize);let t=0;function draw(){ctx.clearRect(0,0,w,h);t+=.003;const cols=Math.ceil(w/sp)+1,rows=Math.ceil(h/sp)+1;for(let r=0;r<rows;r++)for(let cl=0;cl<cols;cl++){const x=cl*sp,y=r*sp,d=Math.sqrt((x-w/2)**2+(y-h/2)**2),wave=Math.sin(d*.008-t*2)*.5+.5;ctx.fillStyle=`rgba(0,212,255,${.03+wave*.04})`;ctx.fillRect(x-.5,y-.5,1,1);}const sy=(t*200)%h,sg=ctx.createLinearGradient(0,sy-20,0,sy+20);sg.addColorStop(0,'transparent');sg.addColorStop(.5,'rgba(0,212,255,.03)');sg.addColorStop(1,'transparent');ctx.fillStyle=sg;ctx.fillRect(0,sy-20,w,40);requestAnimationFrame(draw);}draw();})();
